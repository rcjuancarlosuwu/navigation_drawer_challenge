import 'package:flutter/material.dart';

const hyphenHeight = 1.0;
const hyphenWidth = 7.0;
const hyphenMarginLeft = 30.0;
const tapEffectSize = 50.0;
const tapEffectHalfSize = tapEffectSize / 2;
const productsSliverGridDelegate = SliverGridDelegateWithFixedCrossAxisCount(
  crossAxisSpacing: 15,
  mainAxisSpacing: 7.5,
  crossAxisCount: 2,
  childAspectRatio: 1 / 2,
);

// Responsive
